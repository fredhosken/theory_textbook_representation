# Core pipeline (analysis_server.py, build_db.py, prepare_bio_csv.py,
# framing_analysis.py). Python >= 3.10.
numpy>=1.26
pandas>=2.1
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
networkx>=3.2
openpyxl>=3.1

# GPU NLP passes (analysis_server.py). A CUDA GPU with ~18 GB or more of
# available VRAM is recommended for the fp16 LLM classifier; set
# LLM_CLASSIFIER_LOAD_4BIT = True (requires bitsandbytes) on smaller cards.
torch>=2.1
transformers>=4.40
sentence-transformers>=2.7
accelerate>=0.30

# Optional: faster LLM inference for the framing classifier.
# vllm

# Optional: 4-bit quantisation of the LLM classifier.
# bitsandbytes

# Extraction examples (examples/extraction/) additionally require:
# pdfplumber>=0.10
# spacy>=3.7        (plus: python -m spacy download en_core_web_lg)
# tqdm
